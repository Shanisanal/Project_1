# String Duplicate Removal Program 

## Overview 
This project demonstrates a simple C program that removes duplicate characters from a string.

## Files 
- `main.c`: Implements duplicate removal logic. 
- `.gitignore`: Configured to exclude executable files.

## Compile & Run 
- gcc main.c -o main 
- ./main.exe